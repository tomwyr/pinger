import 'package:flutter/material.dart';

class SessionDetailsResults extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SliverToBoxAdapter(
      child: Text("Results"),
    );
  }
}
